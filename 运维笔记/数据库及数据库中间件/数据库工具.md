1.ShardingSphere

| 方式                     | 适用场景                                               | 你的角色（运维）关注点                     |
| ------------------------ | ------------------------------------------------------ | ------------------------------------------ |
| **ShardingSphere-JDBC**  | 嵌入应用，Java项目里用的库（Java框架）                 | 你不需要接触这部分，主要是开发用的         |
| **ShardingSphere-Proxy** | 独立的中间件进程，部署在服务器上，应用通过它访问数据库 | 你主要关注这个，跟语言无关，配置和运维为主 |

2.yearning

不仅仅是审核工具，规范sql，还能生成事务回滚语句（这是非常重要的）

限权+防呆

如果大量误操作（比如误更新大量记录），借助备份还原出来的临时库做对比，如果是写入极其频繁的业务表（那就GG，需要宕机一段时间）

实际上做完sql规范及权限控制及审计审核流程之后，备份可能在整个业务生命周期都用不上了，当然对应业务表逻辑备份还是用的上的